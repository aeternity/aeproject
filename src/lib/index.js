const networks = require('./networks.json');
const wallets = require('./wallets.json');
const utils = require('./utils');

module.exports = {
  utils: utils,
  networks: networks,
  wallets: wallets
}
