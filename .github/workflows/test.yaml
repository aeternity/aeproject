name: ae-projects-js CI

on: push

jobs:
  test:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: install
      run: |
        npm ci
        npm run develop
    # - name: Node
    #   run: npm run mocha -- test-expensive/node.js
    # - name: Contracts
    #   run: npm run mocha -- test-expensive/contracts.js
    - name: Compatibility tests
      run: npm run mocha -- test-expensive/compatibility-tests.js
    # - name: Shape
    #   run: npm run mocha -- test-expensive/shape-*.js
    # - name: Other tests
    #   run: npm run aetest