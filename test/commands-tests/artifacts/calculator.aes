contract Calculator =

    record state = {
      index : int}

    stateful entrypoint init() = 
      { index = 5 }

    entrypoint sum(a: int, b: int) = a + b

    entrypoint sub(a: int, b: int) = a - b